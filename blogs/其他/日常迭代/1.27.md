---
tags: [AI]
date: 26.1.27
---

# claude code

体验了 claude code， cli agent， 能够读取文件，搜索网页，但是 自带的模型太贵，只能套壳用国内的模型。

下载了claude-code-proxy 用于套内部每月20元免费的模型，流程跑通了，但是发现效果和模型关系比较大，模型还有 rpm（每分钟调用次数）限制，而且大部分都设置的比较小，根本不够claude-code调用的，很烦。

# sanguokill

三国杀使用ai开发陷入瓶颈了， 一个功能怎么都调不出来，很烦。