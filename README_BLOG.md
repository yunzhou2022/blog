# 云粥的博客 - Next.js 版本

基于 Next.js 14 构建的现代化博客系统，自动读取并展示你的 Markdown 文章。

## 功能特性

- ✨ 现代化的 UI 设计，支持深色模式
- 📝 自动读取 Markdown 文件并渲染
- 🎨 美观的文章列表和详情页
- 📱 响应式设计，支持移动端
- ⚡ 基于 Next.js App Router，性能优秀
- 🖼️ 自动处理文章中的图片路径

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000) 查看博客。

### 3. 构建生产版本

```bash
npm run build
npm start
```

## 项目结构

```
blog/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # 根布局
│   ├── page.tsx           # 首页（文章列表）
│   ├── posts/             # 文章详情页
│   │   └── [slug]/
│   │       └── page.tsx
│   └── globals.css        # 全局样式
├── lib/
│   └── posts.ts           # 文章读取和处理工具
└── package.json
```

## 文章格式

文章支持标准的 Markdown 格式，可以包含：

- 标题、段落、列表
- 代码块（支持语法高亮）
- 图片（相对路径会自动处理）
- 链接、引用等

## 图片处理

文章中的图片如果是相对路径（如 `image.png`），会自动转换为 `/年份/图片名` 的格式。

例如：`2024/24-11-25.md` 中的 `![](image.png)` 会被转换为 `/2024/image.png`。

## 部署

### Vercel 部署（推荐）

1. 将代码推送到 GitHub
2. 在 [Vercel](https://vercel.com) 导入项目
3. 自动部署完成

### 其他平台

```bash
npm run build
npm start
```

## 技术栈

- **Next.js 14** - React 框架
- **TypeScript** - 类型安全
- **Tailwind CSS** - 样式框架
- **remark** - Markdown 处理
- **gray-matter** - Frontmatter 解析
- **date-fns** - 日期格式化

## 自定义

### 修改博客标题和描述

编辑 `app/layout.tsx` 中的 metadata：

```typescript
export const metadata: Metadata = {
  title: '你的博客标题',
  description: '你的博客描述',
}
```

### 修改样式

编辑 `app/globals.css` 和 `tailwind.config.js` 来自定义样式。

### 添加新功能

- 在 `lib/posts.ts` 中添加文章处理逻辑
- 在 `app/` 目录下创建新页面
- 使用 Next.js 的 API Routes 创建后端接口

## 许可证

MIT

